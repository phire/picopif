cargo-features = ["per-package-target"]

[workspace]
members = [
    "chainloader",
    "chainload-app",
    "client",
]
#default-members = ["client"]
resolver = "2"

[profile.release]
debug = true
opt-level = 's'
codegen-units=1
lto = true

[profile.dev]
opt-level = 1
